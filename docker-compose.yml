services:
  kamailio:
    build: .
    container_name: kamailio
    env_file:
      - .env
    ports:
      - "50873:50873/tcp"
    restart: unless-stopped
    command: ["kamailio", "-DD", "-E", "-f", "/etc/kamailio/kamailio.cfg"]
